import{describe,expect,it}from'vitest';import{createGame,guess,INITIAL_SCANS,randomSecret,scan}from'./engine';import{heatFor}from'./heat';import{scoreFor}from'./scoring';import{dailySetup}from'./daily';
describe('engine',()=>{it('includes target boundaries',()=>{expect(randomSecret(25,()=>0)).toBe(1);expect(randomSecret(25,()=>.99999)).toBe(25)});it('starts with three scans',()=>expect(createGame('quick',25).scans).toBe(INITIAL_SCANS));it('scan stays in bounds and avoids duplicates',()=>{const g=scan(createGame('quick',25),1);expect(new Set(g.revealed).size).toBe(g.revealed.length);expect(g.revealed.every(n=>n>=1&&n<=25)).toBe(true)});it('wrong guess adds scan and duplicate does not count',()=>{const g={...createGame('quick',25),secret:10};const a=guess(g,2);expect(a.game.scans).toBe(4);expect(guess(a.game,2).game.guesses).toHaveLength(1)});it('correct guess ends game',()=>{const g={...createGame('quick',25),secret:10};expect(guess(g,10).game.completed).toBe(true)})});
describe('heat',()=>{it('classifies exact only as blazing',()=>expect(heatFor(10,10,25)).toBe('blazing'));it('covers thresholds',()=>{expect(heatFor(1,100,100)).toBe('cold');expect(heatFor(96,100,100)).toBe('blazing');expect(heatFor(90,100,100)).toBe('hot')})});
describe('score and daily',()=>{it('uses formula safely',()=>{expect(scoreFor(100,3)).toBe(Math.round(1000*Math.log2(100)/3));expect(scoreFor(100,0)).toBe(0);expect(Number.isFinite(scoreFor(NaN,1))).toBe(true)});it('daily is deterministic and dates vary',()=>{expect(dailySetup('2026-09-17')).toEqual(dailySetup('2026-09-17'));expect(dailySetup('2026-09-17')).not.toEqual(dailySetup('2026-09-18'))})});
