import type {Game,Result,Settings,Stats} from '../game/types';
const KEY='number-hunt:v1'; export const defaults:{stats:Stats;settings:Settings;active:null}={stats:{version:1,games:0,completed:0,totalGuesses:0,bestGuesses:null,bestScore:0,bySize:{},distribution:{},dailyHistory:{},dailyStreak:0,longestDailyStreak:0},settings:{haptics:true,sound:false,onboarded:false,installDismissed:false},active:null};
export interface Data {stats:Stats;settings:Settings;active:Game|null}
export function load():Data{try{const p=JSON.parse(localStorage.getItem(KEY)||'null');if(!p||p.stats?.version!==1)return structuredClone(defaults);return{stats:{...defaults.stats,...p.stats},settings:{...defaults.settings,...p.settings},active:p.active?.version===1?p.active:null}}catch{return structuredClone(defaults)}}
export function save(data:Data){try{localStorage.setItem(KEY,JSON.stringify(data))}catch{/* storage unavailable: game remains playable */}}
export function record(stats:Stats,r:Result,officialDaily=true):Stats{const key=String(r.size),bucket=String(Math.min(9,r.guesses.length));const next:Stats={...stats,completed:stats.completed+1,totalGuesses:stats.totalGuesses+r.guesses.length,bestGuesses:stats.bestGuesses===null?r.guesses.length:Math.min(stats.bestGuesses,r.guesses.length),bestScore:Math.max(stats.bestScore,r.score),bySize:{...stats.bySize,[key]:{games:(stats.bySize[key]?.games||0)+1,best:Math.max(stats.bySize[key]?.best||0,r.score)}},distribution:{...stats.distribution,[bucket]:(stats.distribution[bucket]||0)+1}};if(r.mode==='daily'&&r.date&&officialDaily){const prior=stats.lastDaily?new Date(stats.lastDaily+'T12:00:00'):null,cur=new Date(r.date+'T12:00:00'),consecutive=prior?Math.round((cur.getTime()-prior.getTime())/86400000)===1:false;next.dailyHistory={...stats.dailyHistory,[r.date]:r};next.dailyStreak=stats.lastDaily===r.date?stats.dailyStreak:consecutive?stats.dailyStreak+1:1;next.longestDailyStreak=Math.max(stats.longestDailyStreak,next.dailyStreak);next.lastDaily=r.date}return next}
